# Copy this file to .env and fill in real values for development
# Do NOT commit your real .env to git

# MongoDB connection string (use MongoDB Atlas or local)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/queue_manage
MONGODB_DB=queue_manage

# JWT secret used to sign tokens (change for production)
JWT_SECRET=your_jwt_secret_here

# Server port
PORT=3000
